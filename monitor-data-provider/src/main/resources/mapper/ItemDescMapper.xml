<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.monitor.data.dao.ItemDescDao">

  <!--基础字段映射关系 开始-->	
	  <resultMap id="BaseResultMap" type="ItemDesc">
		<result column="id" property="id" jdbcType="BIGINT"/>
		<result column="item_id" property="itemId" jdbcType="BIGINT"/>
		<result column="item_desc" property="itemDesc" jdbcType="VARCHAR"/>
		<result column="created" property="created" jdbcType="TIMESTAMP"/>
		<result column="updated" property="updated" jdbcType="TIMESTAMP"/>
	  </resultMap>
	<!--基础字段映射关系 结束-->
	<resultMap id="ResultMapWithItemDesc" type="ItemDesc" extends="BaseResultMap" >
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by GenerateCode.Src, do not modify.
    -->
  </resultMap>
	
  <!--字段 Base_Column 开始-->
	  <sql id="Base_Column_List">
	  item_id,item_desc,created,updated,id
	  </sql>
  <!--字段 Base_Column 结束-->
  
  <sql id="limit_sql_str">
        <if test="maxResults > 0">
            limit #firstResult, #maxResults
        </if>
    </sql>

  <!-- 插入商品描述表，可空字段过滤null值(开始) -->
  <insert id="insertByItemDesc" parameterType="ItemDesc" useGeneratedKeys="true" keyProperty="id">
    <!--自增ID不能显示增加 要手工去掉-->
    insert into item_desc
    <trim prefix="(" suffix=")" suffixOverrides=","> 
		item_id,
      <if test="itemDesc != null and itemDesc !=''">
		item_desc,
      </if>
      <if test="created != null and created !=''">
		created,
      </if>
      <if test="updated != null and updated !=''">
		updated,
      </if>
		id,
   </trim>
     values 
     <trim prefix="(" suffix=")" suffixOverrides=",">
		#{itemId,jdbcType=BIGINT},
        <if test="itemDesc  != null and itemDesc !=''">
		#{itemDesc,jdbcType=VARCHAR},
	    </if>
        <if test="created  != null and created !=''">
		#{created,jdbcType=TIMESTAMP},
	    </if>
        <if test="updated  != null and updated !=''">
		#{updated,jdbcType=TIMESTAMP},
	    </if>
		#{id,jdbcType=BIGINT},
     </trim>
  </insert>
  <!-- 插入商品描述表，可空字段过滤null值(结束) -->

  <!-- 根据主键逻辑删除(开始) -->
  <delete id="deleteByPrimaryKey" parameterType="Long">
    update item_desc set yn=1 where 
	       id = #{id,jdbcType=BIGINT}
  </delete>
  <!-- 根据主键逻辑删除(结束) -->

  <!-- 删除商品描述表(开始) -->
  <delete id="delete" parameterType="ItemDesc">
     update item_desc set yn=1 where is_delete=0
	 <if test="itemId != null and itemId != ''" >
        and item_id = #{itemId,jdbcType=BIGINT}
    </if>
	 <if test="itemDesc != null and itemDesc != ''" >
        and item_desc = #{itemDesc,jdbcType=VARCHAR}
    </if>
	 <if test="created != null and created != ''" >
        and created = #{created,jdbcType=TIMESTAMP}
    </if>
	 <if test="updated != null and updated != ''" >
        and updated = #{updated,jdbcType=TIMESTAMP}
    </if>
	 <if test="id != null and id != ''" >
        and id = #{id,jdbcType=BIGINT}
    </if>
  </delete>
  <!-- 删除商品描述表(结束) -->


  <!-- 更新商品描述表 忽略传入空值(开始) -->
  <update id="updateByItemDesc" parameterType="ItemDesc">
    update item_desc
    <set>
    <trim suffix="" suffixOverrides="," >
    <if test="itemId != null and itemId !=''">
        item_id = #{itemId,jdbcType=BIGINT},
    </if>
    <if test="itemDesc != null and itemDesc !=''">
        item_desc = #{itemDesc,jdbcType=VARCHAR},
    </if>
    <if test="created != null and created !=''">
        created = #{created,jdbcType=TIMESTAMP},
    </if>
    <if test="updated != null and updated !=''">
        updated = #{updated,jdbcType=TIMESTAMP},
    </if>
    <if test="id != null and id !=''">
        id = #{id,jdbcType=BIGINT},
    </if>
    </trim>
    </set>
    <trim prefix="where" prefixOverrides="AND | OR" >
    <if test="itemId != null and itemId !=''" >
      and item_id = #{itemId,jdbcType=BIGINT}
    </if>
    <if test="itemDesc != null and itemDesc !=''" >
      and item_desc = #{itemDesc,jdbcType=VARCHAR}
    </if>
    <if test="created != null and created !=''" >
      and created = #{created,jdbcType=TIMESTAMP}
    </if>
    <if test="updated != null and updated !=''" >
      and updated = #{updated,jdbcType=TIMESTAMP}
    </if>
    <if test="id != null and id !=''" >
      and id = #{id,jdbcType=BIGINT}
    </if>
    </trim>
  </update>  
  
  <!-- 更新商品描述表 忽略传入空值(开始) -->
  <select id="queryItemDesc" parameterType="ItemDesc" resultMap="BaseResultMap">
    select 
	<include refid="Base_Column_List"/>
	from item_desc 
	<where> 
    	is_delete=0
    <if test="itemId != null and itemId != ''" >
        and item_id = #{itemId,jdbcType=BIGINT}
    </if>
    <if test="itemDesc != null and itemDesc != ''" >
        and item_desc = #{itemDesc,jdbcType=VARCHAR}
    </if>
    <if test="created != null and created != ''" >
        and created = #{created,jdbcType=TIMESTAMP}
    </if>
    <if test="updated != null and updated != ''" >
        and updated = #{updated,jdbcType=TIMESTAMP}
    </if>
    <if test="id != null and id != ''" >
        and id = #{id,jdbcType=BIGINT}
    </if>
    </where>
  </select>  
  <!-- 更新商品描述表 忽略传入空值 -->    
</mapper>